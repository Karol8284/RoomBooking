
RoomBooking.sln
│
├── RoomBooking.CORE          ← Class Library
├── RoomBooking.Infrastructure ← Class Library  
├── RoomBooking.Shared        ← Class Library
│
├── RoomBooking.API           ← ASP.NET Core Web API
├── RoomBooking.HybridApp     ← MAUI Blazor Hybrid
├── RoomBooking.WebAssembly   ← Blazor WASM
│
├── RoomBooking.AppHost       ← .NET Aspire
└── RoomBooking.Tests         ← xUnit


Technologie per projekt:
RoomBooking. ... : 
CORE → C# 13, .NET 9 (lub .NET 10 Preview jeśli chcesz pokazać odwagę)
Infrastructure → EF Core 9, PostgreSQL (via Npgsql)
API → ASP.NET Core 9, Minimal APIs, Scalar (zamiast Swaggera — nowszy standard)
HybridApp → .NET MAUI + Blazor Hybrid
WebAssembly → Blazor WASM (.NET 9)
Tests → xUnit, FluentAssertions, NSubstitute
AppHost → .NET Aspire 9
Shared/Contracts → Class Library .NET 9